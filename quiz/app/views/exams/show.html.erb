<p>
	<strong>Quiz Title:</strong>
	<%= @exam.title %>
</p>

<strong>Questions in this Quiz:</strong>
<ul>
	<% @exam.problems.each do |prob| %>
		<li><%= prob.question %> = <%= prob.answer %> | <%= link_to "Edit", edit_problem_path(prob) %></li>
	<% end %>
</ul>

<%= link_to "Destroy this Quiz", exam_path(@exam), method: :delete, data: { confirm: "Are you sure?" } %><br />
<%= link_to "Edit this Quiz", edit_exam_path(@exam) %><br />
<%= link_to "See all the Quizzes", exams_path %>