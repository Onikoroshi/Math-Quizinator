<h1>Here are your results!</h1>
<h2>for taking Quiz "<%= @exam.title %>"</h2>

<h3>You got <%= @score %> out of <%= @results.length %>!</h3>

<table>
  <tr>
    <th>No.</th>
    <th>Question</th>
    <th>Correct Answer</th>
    <th>Your Answer</th>
  </tr>

  <% @results.each_with_index do |quest, index| %>
    <% if quest[3] %><tr class="bold"><% else %><tr><% end %>
      <td><%= index+1 %></td>
      <td><%= quest[0] %></td>
      <td><%= quest[1] %></td>
      <td><%= quest[2] %></td>
    </tr>
  <% end %>
</table>

<h3><%= link_to 'Retake this Quiz', take_exam_path(@exam) %></h3>